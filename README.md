# XSV

A csv (comma seperated values) parser. You provide a `.csv` file which then gets printed in a nice formatted way (similar to speadsheet programs). It is called `xsv` as I eventually want to make it so that you can seperate with different deliminators like spaces or something but for now it is hardcoded to only be commas.
